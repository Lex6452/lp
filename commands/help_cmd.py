from pyrogram import Client, filters
from pyrogram.types import Message
from utils.filters import simple_cmd_filter

async def help_cmd(client: Client, message: Message):
    """Выводит список всех доступных команд с описаниями, разделённый по категориям."""
    try:
        help_text = """
📚 **Список команд бота**

**Общие команды**
- `.type <текст>` — Анимация набора текста по символам.
- `.hack` — Анимация взлома Пентагона с поиском данных об НЛО.
- `.speedtest` — Выполняет тест скорости интернета.

**Шаблоны**
- `.+шаб <имя>` — Сохраняет текст из ответа на сообщение как шаблон.
- `.шаб <имя>` — Выводит сохранённый шаблон по имени.
- `.-шаб <имя>` — Удаляет шаблон по имени.
- `.шабы` — Показывает список всех сохранённых шаблонов.

**Удаление сообщений**
- `.дд <число>` — Удаляет указанное количество ваших сообщений в чате.
- `.дд- <число>` — Редактирует и затем удаляет указанное количество ваших сообщений.

**Голосовые сообщения**
- `.+гс <имя>` — Сохраняет голосовое сообщение из ответа под указанным именем.
- `.гс <имя>` — Отправляет сохранённое голосовое сообщение по имени.
- `.-гс <имя>` — Удаляет сохранённое голосовое сообщение по имени.
- `.гсы` — Показывает список всех сохранённых голосовых сообщений.

**Тест скорости**
- `.speed` — Показывает список серверов для тестирования скорости с замаскированными IP.
- `.speed <ID>` — Выполняет тест скорости на сервере с указанным ID.
- `.+speed <имя> <url>` — Добавляет сервер для тестирования скорости.
- `.-speed <ID>` — Удаляет сервер с указанным ID.

**Анимации**
- `.+анимка <имя>` — Сохраняет анимацию (кадры, разделённые #$) из ответа на сообщение.
- `.анимка <имя>` — Воспроизводит сохранённую анимацию, отображая кадры с задержкой.
- `.-анимка <имя>` — Удаляет сохранённую анимацию по имени.
- `.анимки` — Показывает список всех сохранённых анимаций.

**Видеокружочки**
- `.+кружочек <имя>` — Сохраняет видеокружочек из ответа под указанным именем.
- `.кружочек <имя>` — Отправляет сохранённый видеокружочек по имени.
- `.-кружочек <имя>` — Удаляет сохранённый видеокружочек по имени.
- `.кружочки` — Показывает список всех сохранённых видеокружочков.

**Онлайн**
- `.+онлайн` — Включает вечный онлайн.
- `.-онлайн` — Отключает вечный онлайн.
- `.онлайн` — Статус вечного онлайна.

**Справка**
- `.help` — Показывает этот список команд.
"""
        await message.edit(help_text)
    except Exception as e:
        await message.edit(f"⚠️ Ошибка: {str(e)}")

def register(app: Client):
    """Регистрирует обработчик команды .help."""
    app.on_message(simple_cmd_filter("help"))(help_cmd)
