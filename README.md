# lp

Обновление систем
```shell
sudo apt-get update
```

Установка ffmpeg
```shell
sudo apt-get install -y ffmpeg
```

Установка библиотек
```shell
pip3 install pyrogram psycopg2 asyncio tgcrypto httpx Pillow
```

Запуск
```shell
python3 main.py
```
